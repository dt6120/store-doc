{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "dotenv": "^11.0.0",
    "ethereum-waffle": "^3.3.0",
    "ethers": "^5.2.0",
    "hardhat": "^2.3.0",
    "hardhat-deploy": "^0.7.9"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "local": "npx hardhat node",
    "deploy": "npx hardhat deploy --export ./artifacts.json",
    "deploy-mumbai": "npx hardhat deploy --export ./artifacts.json --network mumbai"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.4.2"
  }
}
